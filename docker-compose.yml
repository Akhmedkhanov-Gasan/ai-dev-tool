services:
  validator:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    environment:
      PYTHONPATH: /app
      AI_AGENT_PROVIDER_URL: http://host.docker.internal:11434/api/generate
      AI_AGENT_MODEL: qwen3-coder:30b
